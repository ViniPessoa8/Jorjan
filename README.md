# Backend - Flask

- Setup:
    - ```source bin/activate```
    - ```pip install -r requiremets.tx```

- Running*:
    - ```flask run```


*Obs.: Sempre rodar com o venv ativado (```source bin/activate```)